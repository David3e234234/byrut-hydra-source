name: Update byrut.json

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Режим запуска"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - news
          - check-updates

permissions:
  contents: write

concurrency:
  group: update-byrut
  cancel-in-progress: false

jobs:
  news:
    if: github.event_name == 'schedule' || inputs.mode == 'news'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Scrape news
        run: |
          node scrape.mjs --pages 5 --time-limit 20
          bash publish.sh "chore: scrape news pages [skip ci]"

  catalog:
    needs: news
    if: >-
      ${{
        always() &&
        (github.event_name == 'schedule' || inputs.mode == 'all') &&
        (needs.news.result == 'success' || needs.news.result == 'skipped')
      }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Scrape catalog
        run: |
          node scrape.mjs --all --limit 2500 --time-limit 255
          bash publish.sh "chore: scrape catalog batch [skip ci]"

  check-updates:
    needs: [news, catalog]
    if: >-
      ${{
        always() &&
        (github.event_name == 'schedule' || inputs.mode == 'check-updates') &&
        (needs.news.result == 'success' || needs.news.result == 'skipped') &&
        (needs.catalog.result == 'success' || needs.catalog.result == 'skipped')
      }}
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Check updates
        run: |
          node scrape.mjs --check-updates --updates-limit 1500 --refresh --time-limit 150
          bash publish.sh "chore: check-updates sweep [skip ci]"
